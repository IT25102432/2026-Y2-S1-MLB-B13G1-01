package com.ceylonstar.showtime;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * MovieSphere Cinema Reservation Portal — Showtime Management module.
 * Run this class from IntelliJ (or `mvn spring-boot:run`) and open
 * http://localhost:8080 in your browser.
 */
@SpringBootApplication
public class ShowtimeManagementApplication {
    public static void main(String[] args) {
        SpringApplication.run(ShowtimeManagementApplication.class, args);
    }
}
